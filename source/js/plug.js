"use strict";

document.querySelectorAll(".catalog-user__link").forEach(function (button) {
  button.addEventListener("click", function (evt) {
    evt.preventDefault();

    // Ф-ия открывающая модальное окно, которого нет в макете
  })
})
